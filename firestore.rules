rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Firestore is accessed only by the authenticated server through Firebase Admin.
    // Admin SDK requests bypass these rules; browser access is denied by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
